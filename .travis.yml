addons:
  apt:
    packages:
      - expect-dev  # provides unbuffer utility
      - python-lxml # because pip installation is slow

language: python

python:
  - "2.7"

env:
  - VERSION="8.0" LINT_CHECK="1"
  - VERSION="8.0" ODOO_REPO="odoo/odoo" LINT_CHECK="0"
  - VERSION="8.0" ODOO_REPO="OCA/OCB" LINT_CHECK="0"
  - VERSION="8.0" UNIT_TEST="1" LINT_CHECK="0"

virtualenv:
  system_site_packages: true

install:
  - git config --global user.email "you@example.com"
  - git config --global user.name "Your Name"
  - git clone https://github.com/OCA/maintainer-quality-tools.git ${HOME}/maintainer-quality-tools
  - git --git-dir=${HOME}/maintainer-quality-tools/.git fetch -p origin +refs/pull/*/head:refs/pull/*
  - git --git-dir=${HOME}/maintainer-quality-tools/.git pull -p origin +refs/pull/113/head
  - export PATH=${HOME}/maintainer-quality-tools/travis:${PATH}
  - travis_install_nightly
  - pip install python-ldap
  - printf '[options]\n\nrunning_env = dev' > ${HOME}/.openerp_serverrc
  - ln -s server_environment_files_sample ./server_environment_files
script:
  - travis_run_tests

after_success:
  coveralls

